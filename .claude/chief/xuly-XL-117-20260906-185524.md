Điều tra xong — kết luận: báo động lành tính, không mất dữ liệu. Báo cáo:

✅ ĐÃ LÀM · Điều tra alert "pro parse guard" của wc-open-scraper

**Không phải layout đổi — là giải Pro cá nhân ĐÃ ĐÁNH XONG.** Trang `/pwc2026/live` chỉ chứa trận `scheduled`/`in_progress`; 5 nội dung Pro kết thúc từ 2-3/09 nên giờ /live chỉ còn các trận đồng đội (`*_team_coed`, hôm nay là ngày chung kết đội tuyển). Guard thấy 0 trận Pro → nổ đúng thiết kế, "kept existing rows" là fail-safe đúng.

**Dữ liệu nguyên vẹn, đã đối chiếu từng nội dung với nguồn brackets:**
- DB `wc_pro_matches`: 714 completed = nguồn sporttora chính xác 100% (đơn nam 194/194, đơn nữ 86/86, đôi nam 166/166, đôi nữ 90/90, mixed 178/178), đủ cả 5 trận Grand Final.
- Không cần làm gì thêm — kết quả finals đã sync từ trước khi /live trống.

**Lưu ý:** alert này không có cooldown (gọi `alert()` trực tiếp mỗi chu kỳ cron 1 phút) nên có thể lặp tới 21:59 tối nay; sau đó window đóng vĩnh viễn (giải hết 06/09) → worker tự im, không cần vá/redeploy.

Còn treo từ sáng: parser vẫn bỏ qua 32 tie knockout đội tuyển (chung kết m126 đang diễn ra 18:30). Muốn có data knockout thì nhắn: `/xuly sửa parser knockout wc-open rồi deploy`