✅ ĐÃ LÀM · Note việc sửa worker cho sáng mai + chẩn đoán XL-105

**1. Note sáng mai:** đã thêm mốc `2026-09-04 WC-TIE-INGEST` vào `docs/milestones.md` — chief brief 07:30 sáng mai sẽ tự nhắc, kèm luôn kế hoạch 3 bước (`.claude/chief/xl101-plan.md`) và lời dặn tách job ≤15 phút. Format đã verify parse đúng regex của brief.

**2. XL-105 chưa lên homepage — đúng, và lý do là:** phần 2 của XL-105 (tie-ingest) **chưa hề được code** — job trước dừng vì trần 15 phút và chưa có tin `/xuly` nào giao lại. Verify hôm nay: bảng `wc_open_matches` prod = **0 row** (content-range `*/0`), branch `feat/wc-open-tie-ingest` vẫn 0 commit, homepage `WorldCupLiveCard` chỉ đọc pro (`useWcProLive`). Không có data + card không đọc bảng national → không thể hiện, không phải bug mới.

Bằng chứng: REST count `wc_open_matches` = 0 · `git rev-list main..feat/wc-open-tie-ingest` = 0 · mốc WC-TIE-INGEST parse OK.

Sáng mai muốn chạy luôn thì nhắn: `/xuly làm bước 1+2 tie-ingest theo .claude/chief/xl101-plan.md (parser + worker, commit lên feat/wc-open-tie-ingest, chưa deploy)` — xong tiếp tin thứ hai cho bước 3 (card homepage).