🟡 CHỜ DUYỆT · XL-100 — bài "Đội tuyển Việt Nam trước giờ G"
Đã viết lại chuẩn SEO/GEO/AEO: ~940 từ, 10 heading, 3 FAQ, 5 internal link, meta 55B/141B (đạt CHECK 60/160). Bài VI-only, đăng thẳng vào `vi_blog_posts` — không cần deploy. Đã lưu sẵn ở `.claude/chief/xl100-post.json` + script đăng 1 lệnh. Dừng vì publish bài mới thuộc vùng VÀNG.
Duyệt thì nhắn: `/xuly đăng bài xl-100 đã chuẩn bị (chạy .claude/chief/xl100_publish.py rồi verify Googlebot + request indexing)`

✅ ĐIỀU TRA XONG · XL-101 — national livescore thiếu trên homepage
Nguyên nhân 2 tầng: (1) bảng `wc_open_matches` đang TRỐNG — worker wc-open-scraper mới chỉ lấy 64 đội, phần ingest trận đồng đội hẹn "Sep 3" chưa được viết, trong khi sporttora ĐÃ phát live data từ hôm nay; (2) card homepage chỉ đọc bảng Pro cá nhân. Data lấy được rồi: trang delegations server-render đủ tie + tỉ số từng môn + livescore (đã verify: Brazil–Uruguay, VN gặp Chile 08:00, Cayman 10:00, Colombia 12:00). Kế hoạch fix 3 bước đã ghi ở `.claude/chief/xl101-plan.md` — cần 1 job riêng vì phải sửa worker + UI trên nhánh `feat/wc-open-tie-ingest` và deploy.
Làm thì nhắn: `/xuly làm tie-ingest theo .claude/chief/xl101-plan.md`

Tiếp theo Cuong gõ 1 trong 2 câu trên (hoặc cả hai).