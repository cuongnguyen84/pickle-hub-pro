✅ ĐÃ DỪNG · worker wc-open-scraper
Cron prod đã gỡ ngay (`wrangler triggers deploy` với `crons=[]`) — không còn chạy mỗi phút nữa.
Bằng chứng: CF API trả `"schedules": []` · deployments list không có upload code mới hôm nay.

✅ ĐÃ SỬA · thành scraper kết quả national team (code xong, chưa deploy)
- `parseWcOpenTies` đọc tie đồng đội (`open_team_coed`) từ chính trang delegations (0 request thêm), diff-upsert vào `wc_open_matches` — theo đúng kế hoạch xl101, tận dụng WIP có sẵn trong worktree.
- Thêm guard trong code: worker im lặng đến 00:00 CN 06/09 (giờ VN) rồi TỰ chạy lại — nên duyệt deploy lúc nào cũng an toàn, không lo nó cào sớm.
- Test 37/37 xanh, tsc + eslint sạch. Commit `b6e1ca22` trên `feat/wc-open-tie-ingest`, đã push.
- Mốc 06/09 WC-SCRAPER-RESUME đã ghi vào docs/milestones.md (nhắc nếu chưa deploy trước ngày chung kết).

🟡 CHỜ DUYỆT · deploy worker (vùng vàng)
Không deploy trước 06/09 = ngày chung kết đồng đội sẽ KHÔNG có data national. Phần UI homepage (bước 3) vẫn là WIP chưa commit, làm job riêng.

Duyệt thì nhắn: `/xuly deploy wc-open-scraper từ feat/wc-open-tie-ingest (b6e1ca22)`