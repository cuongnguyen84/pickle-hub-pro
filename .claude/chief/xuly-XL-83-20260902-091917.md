✅ ĐÃ LÀM · Điều tra "549 trận pro" + tư vấn gộp/tách

Số thật trong DB: **886 trận pro** (639 PPA Tour + 247 MLP), thuộc **19 giải** (Shenzhen/Beijing/KL 112 trận, HCMC 104, Singapore 104, Tokyo 80…). Quan trọng: ~845 trận trong số đó **đã có trang riêng** `/tran-dau/<slug>` và đã nằm trong sitemap-matches — tức tầng "tách" per-trận đã tồn tại tự động. Cái thiếu là tầng giữa: blog mới có ~1 bài recap (Beijing) + cụm World Cup.

**Khuyến nghị: GỘP THEO GIẢI, không gộp hết, không tách per-trận.**
- ❌ Tách 886 bài per-trận = thin content, không ai search tên trận lẻ bằng tiếng Việt, Google sẽ coi là rác.
- ❌ Gộp tất cả vào 1 bài = không rank được theo tên từng giải, quá dài.
- ✅ Mỗi giải 1 bài VI "Kết quả + recap" (~19 bài, ưu tiên 6 giải PPA Asia vì gần VN), trong bài link xuống các trang `/tran-dau` của giải đó — vừa khớp query người Việt ("kết quả PPA Shenzhen Open 2026"), vừa bơm internal link cứu các trang trận đang mồ côi, vừa khớp SEO plan hiện tại (blog sự kiện VI).
- Data đã có sẵn trong DB nên bài kết quả có thể generate bán tự động từ `matches` + `match_participants`.

Bằng chứng: query prod `matches group by tournament_name, source_provider` → 19 giải / 886 trận; sitemap-matches.xml.ts đã include slug `ppa_tour-*`.

Muốn làm thì nhắn: **"viết bài kết quả cho PPA Shenzhen Open"** (hoặc giải khác) — publish bài mới là vùng 🟡, mình sẽ soạn đủ 4 thay đổi theo checklist rồi dừng chờ duyệt.