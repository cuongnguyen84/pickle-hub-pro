✅ ĐÃ LÀM · I1 — verify wc-open-scraper ngày chung kết

**Tin tốt:** worker ĐÃ deploy sáng nay 09:12 (chắc từ run XL-115 trước khi quá giờ) — cron sống `* * * * *` (CF API schedules ≠ rỗng), `wc_open_matches` prod có 96/96 trận vòng bảng, updated_at nhảy 09:14 → pipeline chạy thật. Mốc WC-SCRAPER-RESUME đã tick kèm bằng chứng trong docs/milestones.md.

**⚠️ Nhưng có lỗ mới:** parser (`parse-ties.ts`) đang **bỏ qua toàn bộ 32 tie knockout có chủ ý** — code ghi "revisit when the knockout starts on Sep 5" mà chưa ai revisit. Nguồn sporttora đã có m96–m127 (chung kết m126 lúc **18:30 hôm nay**), nhưng chúng sẽ KHÔNG bao giờ vào DB → trang kết quả (1.073 click/tuần) trắng data knockout đúng ngày quan trọng nhất.

Sửa cần: parse tie không có groupId + suy round (R16/QF/SF/F) + nới guard 96 tie + test + redeploy worker — quá 15 phút một lượt xuly, và redeploy là vùng vàng.

Duyệt thì nhắn: `/xuly sửa parser knockout wc-open rồi deploy` — làm trước 18:30 mới kịp trận chung kết.