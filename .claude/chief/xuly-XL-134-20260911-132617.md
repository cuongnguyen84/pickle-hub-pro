🟡 CHỜ DUYỆT · dứt điểm bão alert pro-tour-scraper

Nguyên nhân thật (đọc DB prod): worker chạy **mỗi phút** trong ngày KL Cup, ~5% lượt Browser Rendering bị Cloudflare timeout (215 fail / 4453 ok ngày 10/09, nguồn nào cũng dính, phút sau tự ok). Mỗi tick có 1 lỗi ⇒ ghi run "warning", hàm alert dedupe theo run id ⇒ 258 tin Telegram/24h. Không có nguồn nào kẹt thật, chỉ là hiccup hạ tầng bị coi như sự cố.

Đã sửa trong PR #774 (nhánh `fix/pro-tour-scraper-alert-storm`, từ main):
- Worker: lỗi render/payload rỗng = "tạm thời", chỉ báo khi CÙNG nguồn kẹt quá 20 phút; sự cố thật chỉ ghi run mỗi 10 phút; tin nhắn nêu thẳng URL nguồn kẹt.
- errors-telegram-alert: cooldown 1 giờ theo job.
- Tab Logs admin vẫn ghi từng lỗi như cũ.

Bằng chứng: tsc worker 0 lỗi · vitest pro-tour 59/59 · eslint sạch · CI typecheck + npm-audit đã xanh, còn lại đang chạy. Lưu ý PR tạo dưới tài khoản cuongnguyen84 (token bot grep sai dòng).

Vùng vàng: merge main + deploy worker + deploy edge function. Duyệt thì nhắn: **"merge #774 rồi deploy pro-tour-scraper + errors-telegram-alert"**