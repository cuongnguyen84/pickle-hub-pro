XL-100 đã xong và verify đầy đủ. Với XL-101, kế hoạch cần sửa worker + UI + test + deploy — quá lớn cho phần thời gian còn lại của lượt này (trần 15 phút/job, hai lượt trước đều chết vì quá giờ/limit); làm dở rồi timeout sẽ mất luôn báo cáo XL-100. Theo runbook, tôi báo rõ phần còn lại cần một job riêng.

✅ ĐÃ LÀM · XL-105 phần 1 — đăng bài XL-100 "Đội tuyển VN trước giờ G"
Bài đã lên prod tại /vi/blog/doi-tuyen-pickleball-viet-nam-world-cup-2026 (row `f867524e`, lượt trước kịp INSERT trước khi đứt — lượt này verify + đẩy index).
Bằng chứng: curl Googlebot `?nocache=1` → HTTP 200, title + og:image + canonical + hreflang vi/x-default + FAQPage schema đủ, body 936 từ, meta 55B/141B. IndexNow (Bing) đã ping → HTTP 200.
Còn 1 việc tay: vào GSC → URL Inspection → Request Indexing cho URL trên (Google không có API cho blog).

🟡 CHƯA CHẠM · XL-105 phần 2 — triển khai tie-ingest XL-101
Việc này = sửa parser + worker + card homepage + vitest + deploy (kế hoạch chi tiết đã sẵn ở `.claude/chief/xl101-plan.md`). Quá lớn cho phần còn lại của khung 15 phút/lượt — 2 lượt trước (XL-100, XL-101) đều chết vì quá giờ nên tôi không mạo hiểm làm dở rồi mất luôn báo cáo này.
Làm ngay thì nhắn riêng 1 tin: `/xuly làm bước 1+2 tie-ingest theo .claude/chief/xl101-plan.md (parser + worker, commit lên feat/wc-open-tie-ingest, chưa deploy)` — tách bước 3 (UI homepage) thành tin thứ hai để mỗi job lọt khung giờ.

Tiếp theo: Cuong bấm Request Indexing trên GSC, rồi gửi 2 tin `/xuly` như trên để chạy tie-ingest.