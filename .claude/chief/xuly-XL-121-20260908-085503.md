✅ ĐÃ LÀM · Kiểm tra sức khoẻ site (08:55, 08/09)

**Tổng thể: XANH.** Không có sự cố nào đang mở.

- **Web prod:** `/`, `/vi`, `/san`, `/shop`, `/live`, sitemap, bài World Cup results đều 200, title đúng, 0.1–0.7s (Googlebot + người thật). Commit main mới nhất #741 (Permissions-Policy) đã lên prod, header có mặt cả 2 đường bot/người.
- **Supabase:** REST 200, edge fn 200. Edge-redeploy 07:51: 85/85 OK, 0 fail.
- **Cron/job:** 14 job, không job nào fail. 1 cảnh báo nhẹ: `feed-embeds-sync` lúc 08:50 báo "một vài item lỗi" (HTTP vẫn 200, không chi tiết) — thường là embed IG/FB chết link, không ảnh hưởng người dùng.
- **Shop:** 0 đơn mới 24h, 0 đơn quá hạn, 0 claim chuyển khoản chờ xác nhận.
- **Tin tức:** 24 tin mới 24h, hàng đợi dịch 0 lỗi · 0 tồn.
- **Đội agent:** chief 07:34, content 08:30, opssweep 07:54, xuly 08:50 đều chạy đúng lịch; sweep sạch 10 lượt liên tiếp từ 22h tối qua.

Bằng chứng: curl 7 URL prod 200 · edge-redeploy.log `finished: total=85 ok=85 failed=0` · RPC ops_job_health_snapshot 14 job · opssweep.log "0 vấn đề" 22:46→07:54.

Không cần gõ gì thêm. Muốn soi item lỗi của feed-embeds-sync thì nhắn: "xem feed-embeds-sync lỗi gì".