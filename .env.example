# ThePickleHub — local environment template.
# Copy to `.env` and fill in. `.env` is gitignored; NEVER commit real values.
# These VITE_* values are PUBLIC (they ship in the client bundle) — do NOT put
# service_role keys, Resend keys, or any server-side secret in this file.
# Server secrets live in Supabase secrets / Cloudflare Worker secrets.

VITE_SUPABASE_URL=https://ajvlcamxemgbxduhiqrl.supabase.co
VITE_SUPABASE_PROJECT_ID=ajvlcamxemgbxduhiqrl
VITE_SUPABASE_PUBLISHABLE_KEY=

# Legacy aliases (kept for tooling that reads non-prefixed names)
SUPABASE_URL=https://ajvlcamxemgbxduhiqrl.supabase.co
SUPABASE_PUBLISHABLE_KEY=
