name: Apple tests

on:
  pull_request:
    branches: [main]
    paths:
      - "apple/**"
      - ".github/workflows/apple-tests.yml"
  push:
    branches: [main]
    paths:
      - "apple/**"
      - ".github/workflows/apple-tests.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: apple-tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unit-tests:
    timeout-minutes: 30
    runs-on: macos-15

    steps:
      - uses: actions/checkout@v7

      - name: Install XcodeGen
        run: |
          brew install xcodegen
          xcodegen --version
          xcodebuild -version

      - name: Generate Xcode project
        working-directory: apple
        run: |
          cp Config/Secrets.example.xcconfig Config/Secrets.xcconfig
          xcodegen generate
          mkdir -p ThePickleHub.xcodeproj/project.xcworkspace/xcshareddata/swiftpm
          cp Config/Package.resolved ThePickleHub.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved

      - name: Run native release preflight
        working-directory: apple
        run: ./scripts/release_preflight.sh

      - name: Select an available iPhone simulator
        id: simulator
        run: |
          SIMULATOR_ID=$(xcrun simctl list devices available --json | jq -er \
            '[.devices[][] | select(.isAvailable == true and (.name | startswith("iPhone")))] | first | .udid')
          echo "id=$SIMULATOR_ID" >> "$GITHUB_OUTPUT"
          xcrun simctl list devices | grep "$SIMULATOR_ID"
          xcrun simctl shutdown "$SIMULATOR_ID" || true
          xcrun simctl boot "$SIMULATOR_ID"
          xcrun simctl bootstatus "$SIMULATOR_ID" -b

      - name: i18n gates (duplicates, specifiers)
        run: |
          node scripts/native-i18n-gates.mjs duplicates
          node scripts/native-i18n-gates.mjs specifiers

      - name: Run native unit tests
        working-directory: apple
        run: |
          set -o pipefail
          xcodebuild test \
            -project ThePickleHub.xcodeproj \
            -scheme ThePickleHub \
            -destination "platform=iOS Simulator,id=${{ steps.simulator.outputs.id }}" \
            -derivedDataPath "$RUNNER_TEMP/DerivedData" \
            -resultBundlePath "$RUNNER_TEMP/ThePickleHubTests.xcresult" \
            -onlyUsePackageVersionsFromResolvedFile \
            CODE_SIGNING_ALLOWED=NO

      # Chạy lại toàn bộ suite ở locale EN — trước song ngữ, bundle chỉ có vi nên
      # test vô tình chạy vi_US; sau song ngữ locale dưới chân test là en_US.
      # Job này khai báo tường minh cấu hình đó (pre-mortem native-bilingual, sự cố 1).
      - name: Run native unit tests (English locale)
        working-directory: apple
        run: |
          set -o pipefail
          xcodebuild test \
            -project ThePickleHub.xcodeproj \
            -scheme ThePickleHub \
            -destination "platform=iOS Simulator,id=${{ steps.simulator.outputs.id }}" \
            -derivedDataPath "$RUNNER_TEMP/DerivedData" \
            -resultBundlePath "$RUNNER_TEMP/ThePickleHubTests-en.xcresult" \
            -onlyUsePackageVersionsFromResolvedFile \
            -testLanguage en -testRegion US \
            CODE_SIGNING_ALLOWED=NO

      - name: Build unsigned Release with strict concurrency checks
        working-directory: apple
        run: |
          set -o pipefail
          xcodebuild build \
            -project ThePickleHub.xcodeproj \
            -scheme ThePickleHub \
            -configuration Release \
            -destination "platform=iOS Simulator,id=${{ steps.simulator.outputs.id }}" \
            -derivedDataPath "$RUNNER_TEMP/DerivedData-Release" \
            -onlyUsePackageVersionsFromResolvedFile \
            CODE_SIGNING_ALLOWED=NO

      - name: Upload test result on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: apple-test-results
          path: ${{ runner.temp }}/ThePickleHubTests.xcresult
          if-no-files-found: ignore
          retention-days: 7
