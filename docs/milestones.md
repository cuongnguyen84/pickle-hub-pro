# Mốc hẹn ngày (dated milestones)

Nguồn sự thật cho mọi việc "đến ngày X mới làm". Mỗi phiên autonomous chạy
`node scripts/due-milestones.mjs` ở đầu phiên (CLAUDE.md §Critical Workflow Notes);
workflow `milestone-due.yml` ping Telegram mỗi ngày khi còn mốc due chưa tick.

**Luật (từ proposal auto-milestone-run-2026-07, pre-mortem):** mỗi mốc mang **cả
PREDICATE lẫn ngày**. Đến ngày mà predicate chưa thoả → KHÔNG hành động, ghi lý do
defer vào dòng mốc và giữ nguyên `[ ]`. Mốc chỉ nhớ ngày mà quên điều kiện sẽ kích
hoạt quyết định trên dữ liệu chưa tồn tại. Xong việc → tick `[x]` cùng commit với
bằng chứng (PR/report).

Format (script parse bằng regex, giữ đúng): `- [ ] YYYY-MM-DD ID — mô tả`

- [x] 2026-07-24 PERF-05 — DONE 28/07, verdict GIỮ: LCP p75 mobile 2684→2423ms (vào GOOD), CLS 0.68→0.67 POOR cả trước lẫn sau (có sẵn, không phải regression → finding riêng), INP thiếu mẫu. Report: docs/perf-05-report-2026-07-28.md. Nguồn thật = CrUX (giả định "GA4 dimensions đã đăng ký 21/07" SAI — 10 dims đó là funnel/badge, thiếu metric_name/metric_rating; đã đăng ký bổ sung 28/07, không hồi tố).
- [x] 2026-08-05 PERF-05B — ✅ ĐỌC 23/09 (GA4 prop 522556358, event web_vital, 24/08→21/09, country=Vietnam + deviceCategory=mobile): LCP 81.8% good (n=10268) · INP 86.0% good (n=6584) · CLS 81.4% good (n=2539) · FCP 90.7% (n=15024) · TTFB 91.6% (n=14668). Verdict: LCP + INP = XÁC NHẬN GIỮ (≥75% ⇔ p75 đạt). CLS = MÂU THUẪN CrUX (CrUX 28/07 báo CLS p75 0.67 POOR, RUM nói đạt) → điều tra ở PERF-05C. Giả thuyết số 1: n CLS chỉ bằng 25% n LCP — CLS chỉ gửi lúc pagehide, tab mobile bị kill mất beacon → mẫu RUM lệch về phiên "đẹp". Report: growth-tasks/reports/2026-09-23.md.
- [x] 2026-08-23 SEO-CLUSTER-READ — ✅ ĐỌC 23/09 (GSC API sc-domain, query=equals, page dim, 28 ngày 23/08→19/09, so cùng độ dài 28/06→25/07 trước deploy): "pickleball bracket generator" 3→**2 URL** (/tools 6 click/69 impr/pos 7.4, trước 0/40/9.9; /tools/doubles-elimination 0/2/pos 46), bài /blog/how-to-create-pickleball-bracket đã rời query. "pickleball round robin generator" /tools 0/5/pos 33.2 + guide 0/1/pos 60 — gần như không có mặt. Verdict: CHƯA ĐỦ (URL gộp đạt, /tools pos 7.4 > 5) → không sửa gì, đọc lại ở SEO-CLUSTER-READ-2.
- [x] 2026-08-01 THELINE-HARD — FLIPPED 03/08 (PR #534). Predicate thoả: rename→before=0 sửa trước (basePathOf qua git diff --find-renames, verify trên synthetic rename repro); dry-run HARD sạch 5 PR merged (#529-#533) + 14 PR mở kể cả #497 (36 tsx) — 0 vi phạm, 0 false-positive. Rule 2 raw-color vẫn advisory.
- [x] 2026-08-02 UX-07-FUNNEL — ĐỌC XONG 03/08, verdict CLOSE. GA4 property 522556358, window 20/07–02/08: auth_wall_viewed/click/registration_complete = 0 user (VN và all, toàn thời gian từ khi instrumentation live 21/07 #428), trong khi /tools/quick-tables/* có ~210 user-visits/1017 views và organizer_tournament_* chảy đều (VN 10→8→7, 70% complete) → pipeline sống, 0 là số đo thật. Dưới mọi ngưỡng BUILD (0/100 wall users). D5 ngả solution-architect: không ai tới wall vì 0/105 bảng bật requires_registration 60 ngày — bài toán ở thượng nguồn, ĐÓNG UX-07, KHÔNG build guest path. Ghi chú ops: GA4_PROPERTY_ID=522556358 (Admin API project SA đang disabled); index.html gắn debug_mode:true mọi gtag config prod — review riêng.
- [ ] 2026-08-31 WPR-REFRESH — DEFER 23/09 (daily-growth run): ppatour.com/rankings là trang JS, web_fetch của phiên cron không được phép đọc URL này và không có Chrome mở lúc 07:00 → không đối chiếu tay được; KHÔNG đoán hằng số. Dữ liệu hiện lệch 27 ngày (PPA_WPR_FETCHED_AT 2026-08-27). Phần GA4 đã đọc: VN 27/08→21/09 wpr_search_no_result 2 event/1 user, wpr_source_click 2/2 → quá ít, KHÔNG cần email nhắc PPA. Cần một phiên có Chrome. Mô tả gốc: sáng thứ Hai hằng tuần (workflow nhắc lúc 08:05 ICT; đã kích hoạt ngoài lịch lần đầu 2026-08-27): đối chiếu tay src/content/ppa-rankings.ts với ppatour.com/rankings (top-25 ×2 + mọi VĐV cờ vn), sửa hằng số + PPA_WPR_FETCHED_AT, `?nocache=1` ×4 URL. PREDICATE: vẫn CHƯA có thư cho phép từ legal@ppatour.com (có thư → thay bằng đề xuất pipeline tự động, proposal rankings-dupr-wpr-tabs bước 5). CẤM commit script filter (risk D2 điều kiện 4 — hằng số viết tay). Đồng thời đọc GA4 wpr_search_no_result + wpr_source_click (segment VN): no-match cao → soạn email nhắc PPA lần 2 kèm số. Xong → tick, tạo mốc thứ Hai tuần kế tiếp (+7 ngày, tự re-arm).
- [x] 2026-08-10 SEO-SAN-W33 — ✅ CHẠY 23/09: kỳ 14–20/09 = 102 click / 7558 impr / pos 7.7, WoW +13.3% (tuần trước 90) → không cảnh báo. Row đã append docs/ops/seo-san-weekly.md. (Kỳ 04–10/09 đã được đội agent đọc 13/09 nhưng mốc chưa tick.)
- [ ] 2026-09-28 SEO-SAN-W39 — tracker organic /san tuần. Chạy `python3 scripts/seo/gsc_report.py --page-contains /san/` (env GOOGLE_SA_JSON=.claude/secrets.local.gsc-ga4-sa.json) → append row docs/ops/seo-san-weekly.md. PREDICATE: end = hôm nay −3 ngày. Clicks giảm >20% WoW = báo Cuong NGAY. Xong → tick + tạo mốc +7 ngày cùng commit.
- [ ] 2026-09-30 PERF-05C — điều tra CLS mâu thuẫn CrUX vs RUM (xem PERF-05B). Bước: (1) đọc CrUX hiện tại cho origin, phone, CLS p75; (2) nếu CrUX vẫn POOR mà RUM 81% good → đo tỷ lệ phiên có LCP nhưng không có CLS (GA4 metric_id/journey_id) để xác nhận mất beacon pagehide; (3) GA4 customEvent:cls_shift_target top giá trị poor. PREDICATE: không có. Verdict ∈ {RUM THIẾU MẪU → tin CrUX, CrUX ĐÃ CẢI THIỆN → đóng finding, KHÁC → ghi giả thuyết mới}.
- [ ] 2026-10-21 SEO-CLUSTER-READ-2 — đọc lại "pickleball bracket generator" + "pickleball round robin generator" theo TRANG, 28 ngày, query=equals (baseline 23/09: bracket 2 URL, /tools 6/69/pos 7.4; round robin /tools pos 33). Verdict ∈ {THẮNG = ≤2 URL VÀ /tools ≤ pos 5; CHƯA ĐỦ lần 2 → đề xuất việc on-page cho /tools (không chờ thêm); THUA = số URL tăng lại hoặc /tools tụt > pos 10}.
- [x] 2026-08-04 BADGE-TELEMETRY — ✅ ĐỌC 03/08 (GA4 prop 522556358, 21/07→02/08, SA curl): 128 events / 18 users (VN 127/17, sạch bot); funnel Σeligible 136 → Σwith_data 1 → Σshown 0. Badge KHÔNG hiện lần nào — không phải bug (event bắn đều, metrics populate), không phải thiếu traffic (18 user thấy danh sách eligible): đăng ký approved thưa, chưa row nào đạt REG_BADGE_MIN=4 (quick-tables avg eligible 1.01, with_data 0); 0 event 29/07→02/08. Verdict keep/kill: CHƯA THỂ KẾT LUẬN theo predicate (impression-only). Badge muốn sống → đề xuất riêng: hạ REG_BADGE_MIN 4→1-2 hoặc thêm badge_click/holdout.
- [x] 2026-09-04 WC-TIE-INGEST — bước 1+2 XONG 03/09 tối (commit b6e1ca22 trên feat/wc-open-tie-ingest, đã push): `parseWcOpenTies` (src/lib/wc-open/parse-ties.ts, 37/37 vitest xanh) + worker diff-upsert `wc_open_matches`. ĐỒNG THỜI theo lệnh Cuong 03/09: cron prod wc-open-scraper ĐÃ GỠ (`wrangler triggers deploy` crons=[], API xác nhận schedules=[]) vì hết trận đến hết thứ 7 5/9; code thêm guard `Date.UTC(2026,8,5,17)` nên deploy lại lúc nào cũng an toàn, tự chạy lại 00:00 06/09 VN. Còn treo: deploy worker (VÀNG, chờ Cuong duyệt) + bước 3 UI card (WIP chưa commit trong worktree wc-open-ties).
- [x] 2026-09-06 WC-SCRAPER-RESUME — ✅ VERIFY 06/09 09:30 (xuly I1): worker ĐÃ deploy 09:12 VN (CF API modified_on 2026-09-06T02:12:44Z), cron sống `* * * * *` (schedules ≠ []), `wc_open_matches` = 96 row (REST count 0-95/96), updated_at bump 02:14Z sau deploy 2 phút → pipeline chạy. ⚠️ LỖ MỚI PHÁT HIỆN: parse-ties.ts BỎ QUA 32 tie knockout có chủ ý (`if (!group) continue`, comment "revisit when the knockout starts on Sep 5" — chưa revisit); nguồn sporttora ĐÃ có m96–m127 (chung kết m126 18:30 hôm nay) nhưng sẽ KHÔNG vào DB → trang kết quả không có data knockout ngày chung kết nếu không sửa parser + redeploy trong hôm nay. Đã báo Cuong qua Telegram cùng lượt xuly này.
- [ ] 2026-12-10 IG-TOKEN-REAUTH — gia hạn quyền IG cho `feed-embeds-sync` TRƯỚC khi data-access hết hạn ~20/12/2026 (unix 1797737415). Token hiện tại (đặt 21/09/2026) là **Page access token** của Trang TA Pickleball qua app `getlink Ins` (1722033425478159) → trường "Hết hạn" = Không bao giờ, NHƯNG Meta vẫn cắt *data access* sau 90 ngày khi app chưa có Advanced Access. PREDICATE: chạy debug_token thấy `data_access_expires_at` < 14 ngày nữa HOẶC `feed_embed_sources.last_error` có chuỗi "Session has expired"/"access token". Cách làm: Graph Explorer (app getlink Ins, quyền instagram_basic + pages_show_list + pages_read_engagement) → Generate Access Token → debugger → "Mã truy cập mở rộng" (cần mật khẩu FB của Cuong) → `me/accounts?fields=access_token` lấy Page token mới → POST `/v1/projects/ajvlcamxemgbxduhiqrl/secrets` set `IG_ACCESS_TOKEN`. Bối cảnh: token cũ chết 02/09/2026 đúng 60 ngày sau khi dựng job (04/07) và KHÔNG ai biết trong 19 ngày — last_error chỉ nằm trong bảng, không có alert. Cân nhắc xin Advanced Access cho instagram_basic để khỏi lặp lại.
