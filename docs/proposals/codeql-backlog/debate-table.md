## Bảng bất đồng — codeql-backlog

| # | Chủ đề | Các phía | Vòng 2 | Trạng thái | Kết luận |
|---|--------|----------|--------|------------|----------|
| D1 | Alert #24 safeRedirect.ts:35 — bug sống phải fix ưu tiên #1, hay char-class thừa vô hại (fix 1 dòng cho rẻ hoặc dismiss)? | **solution-architect**: Char-class thừa nhưng behavior đúng (reject whitespace+control); sửa 1 dòng rẻ hơn cãi, hoặc dismiss. Xếp vào <br>**risk-auditor**: Bug SỐNG hôm nay: /[ -\s]/ match cả dấu '-' → mọi redirect sau login tới path có hyphen bounce về '/'. GPT-5.6 | **solution-architect**: CONCEDE (`src/lib/auth/safeRedirect.ts:35 — runtime node -e xác nhận /`)<br>**risk-auditor**: HOLD | ✅ RESOLVED_EVIDENCE | Alert #24 là defect sống (mọi path có hyphen bị drop redirect sau login). Fix ưu tiên #1, PR đứng riêng, 1 dòng regex + test: hyphen PASS; //evil.com, /\evil, /%2F, javascript: BLOCK. Mức độ: lỗi UX mất return-to, không phải sập auth. |
| D2 | Alert #14/15/16 xss-through-dom (AdminViBlogEditor, EditLivestreamDialog — admin-only): fix code (validate URL scheme) hay dismiss-with-reason? | **solution-architect**: Fix bằng validate URL scheme (http/https-only) trước khi bind — vừa clear alert vừa là a11y/UX hợp lý (PR4).<br>**risk-auditor**: Dismiss-able: admin-only, self-XSS. Đừng rewrite hành vi chỉ để im CodeQL. | **solution-architect**: CONCEDE (`src/pages/admin/AdminViBlogEditor.tsx:282-283,295 (<img src=`)<br>**risk-auditor**: HOLD | ✅ RESOLVED_EVIDENCE | Dismiss #14/15/16 với reason 'admin-only input, <img src> sink không exec script, <a href> prefix cứng'. Không thêm scheme allow-list (sẽ chặn data:image URI hợp lệ). |

