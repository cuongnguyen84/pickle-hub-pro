## Bảng bất đồng — ds-03-component-standardization

| # | Chủ đề | Các phía | Vòng 2 | Trạng thái | Kết luận |
|---|--------|----------|--------|------------|----------|
| D1 | Thiết kế ratchet gate (DoD b): đếm aggregate baseline toàn repo hay chỉ changed-files/AST + trial report-only? | **solution-architect**: Baseline count toàn repo trong JSON committed, fail nếu TĂNG; per-file/AST là YAGNI cho tới khi thấy lách thật<br>**risk-auditor**: Aggregate count là rủi ro TB#5 (xoá 1 + thêm 1 = xanh giả, miss raw <button>/multiline); phải ESLint AST-aware<br>**pre-mortem**: Sự cố ưu tiên #1: full-tree count flap trên PR không liên quan → gate bị hạ `\|\| true` như theline-audit.yml: | **solution-architect**: CONCEDE (`.github/workflows/theline-audit.yml:27 — full-tree scan chạy`)<br>**risk-auditor**: REFINE<br>**pre-mortem**: REFINE | ✅ RESOLVED_EVIDENCE | Hội tụ 4/4: ratchet CHANGED-FILES tái dùng targetFiles() (check-theline.mjs:42-65) — file đã đổi không được tăng số .tl-btn/raw-element so với base; rollout REPORT-ONLY 2 tuần trước khi enforce (precedent theline-audit.yml:27). solution-architect CONCEDE có bằng chứng. Residual nhỏ về detector (regex vs ESLint AST — risk-auditor và pre-mortem đổi chỗ cho nhau): bắt đầu bằng regex changed-files, nâng lên ESLint no-restricted-syntax nếu 2 tuần report-only lộ false-positive/lách. |
| D2 | Native cùng đợt (intake của Cuong) vs tách release native sau khi web ổn định (mitigation RED của risk-auditor) — và 'thêm component vào TLComponents.swift' có đồng nghĩa 'release App Store' không? | **solution-architect**: Native TLButton ở increment 1, TLBadge/Select/Sheet/Dialog ở increment 4 — cùng các increment với web, không t<br>**risk-auditor**: TLComponents.swift = RED cứng (App Store không có nút revert, 0 UI/snapshot test trong apple-tests.yml); tách  | **solution-architect**: REFINE<br>**risk-auditor**: REFINE | 🔶 OPEN_FOR_CUONG | [ĐỀ XUẤT THỐNG NHẤT CỦA PANEL — chờ Cuong gật] Định vị lại RED giải tan bất đồng: CODE native (TLComponents.swift) merge cùng đợt web như intake Cuong muốn — git-revertable, không phải RED. RED neo vào bước SUBMIT App Store (không revert được), bị chặn tới khi: (1) snapshot/UI test cho DesignSystem/Components incl. .dynamicTypeSize(.accessibility3), (2) TLSheet luôn bọc ScrollView, (3) test tay VI/Dynamic Type/VoiceOver trên máy nhỏ. Release native vốn là cổng thủ công của Cuong nên không xung đột 'cùng đợt'. |
| D3 | Mức độ rủi ro double-submit money-path (risk #1, một nửa lý do RED phần web): có cơ chế thật không khi DB đã có UNIQUE + handler idempotent + OTP single-use? | **risk-auditor**: Cao — migration làm rơi type=/disabled → double-submit → server race confirm cả 2 → overbook sự kiện thu phí (<br>**pre-mortem**: Cơ chế double-submit→2-payment-order KHÔNG tồn tại: payment_orders.registration_id UNIQUE (20260512130001_paym | **risk-auditor**: REFINE<br>**pre-mortem**: HOLD | 🔶 OPEN_FOR_CUONG | [ĐỀ XUẤT THỐNG NHẤT CỦA PANEL — chờ Cuong gật] risk-auditor REFINE hạ risk #1 Cao→Thấp sau khi tự kiểm chứng bằng chứng của pre-mortem (UNIQUE payment_orders.registration_id, handler idempotent, advisory lock DB-01/01c, unique index registrations, OTP single-use — double-submit tệ nhất = 1 click thừa bị server từ chối). Web tier = AMBER (còn risk #3 Select payload). Global RED giữ nguyên, neo vào D2 native submit. |
| D4 | API Button: thêm variant TL mới (tl/tl-primary/tl-green) hay map vào variant shadcn có sẵn (green→default, primary→secondary, base→outline)? Và `loading` prop vào ngay increment 1 hay defer? | **solution-architect**: Mapping không 1:1 (tl-btn.primary = fill --tl-fg kem, không trùng variant nào) → PHẢI thêm 3 variant TL riêng;<br>**ui-ux-critic**: Map vào variant có sẵn: green→default (cùng lime), primary→secondary (kem), base→outline — bảng map tường minh | **solution-architect**: REFINE<br>**ui-ux-critic**: REFINE | 🔶 OPEN_FOR_CUONG | [ĐỀ XUẤT THỐNG NHẤT CỦA PANEL — chờ Cuong gật] Hội tụ 2 chiều có bằng chứng cùng file (the-line.css:35 vs :545-546): CHỈ tl-primary (kem, fill --tl-fg) là variant MỚI bắt buộc; green→default (#b5e853 khớp 1:1) và base→outline TÁI DÙNG. Bảng map tường minh là blocker giữ nguyên (chặn codemod đoán tên lật kem→lime ~30 nút). Residual nhỏ: loading prop — hai bên ĐỔI CHỖ (critic giờ muốn inc.1, architect tự rút về DS-04); mặc định DEFER theo YAGNI, thêm ngay khi increment nào đụng spinner thật. |

### 🔶 Cần anh quyết (3)

**D2 — Native cùng đợt (intake của Cuong) vs tách release native sau khi web ổn định (mitigation RED của risk-auditor) — và 'thêm component vào TLComponents.swift' có đồng nghĩa 'release App Store' không?**

- `solution-architect`: Native TLButton ở increment 1, TLBadge/Select/Sheet/Dialog ở increment 4 — cùng các increment với web, không tách lịch release
- `risk-auditor`: TLComponents.swift = RED cứng (App Store không có nút revert, 0 UI/snapshot test trong apple-tests.yml); tách release native SAU khi web ổn định + thêm snapshot test + test tay VI/Dynamic Type/VoiceOver trước submit

**D3 — Mức độ rủi ro double-submit money-path (risk #1, một nửa lý do RED phần web): có cơ chế thật không khi DB đã có UNIQUE + handler idempotent + OTP single-use?**

- `risk-auditor`: Cao — migration làm rơi type=/disabled → double-submit → server race confirm cả 2 → overbook sự kiện thu phí (SLO 3); cần test double-click = 1 rpc call
- `pre-mortem`: Cơ chế double-submit→2-payment-order KHÔNG tồn tại: payment_orders.registration_id UNIQUE (20260512130001_payment_orders.sql:24), create-payment-order/handler.ts:146-178 idempotent xử lý đúng race, OTP single-use nên verify lần 2 fail — đã chủ động loại khỏi pre-mortem

**D4 — API Button: thêm variant TL mới (tl/tl-primary/tl-green) hay map vào variant shadcn có sẵn (green→default, primary→secondary, base→outline)? Và `loading` prop vào ngay increment 1 hay defer?**

- `solution-architect`: Mapping không 1:1 (tl-btn.primary = fill --tl-fg kem, không trùng variant nào) → PHẢI thêm 3 variant TL riêng; thêm loading?: boolean ngay increment 1 để dọn đường DS-04/A11Y
- `ui-ux-critic`: Map vào variant có sẵn: green→default (cùng lime), primary→secondary (kem), base→outline — bảng map tường minh là Blocker B1; loading prop hạ xuống tuỳ chọn S4, DoD chỉ cần GIỮ hành vi loading hiện có, đừng nống scope (GPT-5.6 ngược lại muốn loading first-class)


debate-ledger: 4 bất đồng · 1 giải quyết bằng bằng chứng · 3 còn mở cho Cuong
✅ Luật đối chất OK. 3 bất đồng còn mở — đó là kết quả hợp lệ, không phải lỗi.
