## Bảng bất đồng — rls-anon-select-hardening

| # | Chủ đề | Các phía | Vòng 2 | Trạng thái | Kết luận |
|---|--------|----------|--------|------------|----------|
| D1 | Nút 'Copy invite code' (TeamRosterManager.tsx:358-366): giữ và nuôi bằng RPC SECURITY DEFINER, hay xóa hẳn vì là dead-end? | **solution-architect**: GIỮ qua RPC team_match_get_invite_code (captain/organizer). Là lời giải cho ràng buộc ROLE; effort 1 RPC. (Opt<br>**risk-auditor**: GIỮ qua RPC — coi mất nút chia sẻ mã là rủi ro #3 (TB), migration PHẢI kèm RPC + rewire client.<br>**ui-ux-critic**: XÓA nút + prop + toast. Đã verify: không route/input nào tiêu thụ team_match_teams.invite_code (join flow là b | **solution-architect**: CONCEDE (`src/hooks/useTeamMatchTeams.ts:615 — useTeamByInviteCode là `)<br>**risk-auditor**: CONCEDE (`src/hooks/useTeamMatchTeams.ts:615 — grep toàn src/ ra đúng `)<br>**ui-ux-critic**: HOLD | ✅ RESOLVED_EVIDENCE | XÓA nút copy invite-code + prop inviteCode + toast (TeamRosterManager.tsx:358-366, TeamDetailSheet.tsx:133). KHÔNG xây RPC. Đường mời còn sống = InviteTeamDialog (email, edge fn service_role). Architect + risk-auditor cùng CONCEDE với evidence độc lập. |
| D2 | Cơ chế ship: 1 migration atomic áp cùng lúc, hay bắt buộc thứ tự deploy-web-live-trước → áp REVOKE sau? (Crux factual: bundle CŨ select('*') sau REVOKE → 42501 chết cả query, hay degrade êm?) | **solution-architect**: Atomic 1 PR; sau r2: nhận ordering là 'vệ sinh free' nhưng xếp Sự cố 1 xuống lỗi mỹ phẩm vì tin claim degrade-<br>**ui-ux-critic**: Cùng 1 PR (blocker #1); r2: 'same PR' và 'sequenced apply' tương thích — nghiêng theo risk-auditor về thứ tự á<br>**risk-auditor**: r1: expand-then-contract 2 file; r2 REFINE: 1 file đủ, bất biến là THỨ TỰ — deploy web narrow, xác nhận live, <br>**pre-mortem**: HOLD: select('*') → whole-request 403 → throw → bảng đấu live trắng trong cửa sổ 3-5 phút. PostgREST schema ca | **solution-architect**: REFINE<br>**ui-ux-critic**: REFINE<br>**risk-auditor**: REFINE<br>**pre-mortem**: HOLD | 🔶 OPEN_FOR_CUONG | **cần Cuong quyết** |
| D3 | Verdict tier cho scope ĐÃ thu hẹp (chỉ revoke invite_code, native 0 file): RED hay thấp hơn? | **solution-architect**: Dự kiến YELLOW — migration RLS/grant không chạm auth/payments/config.toml, native không đổi.<br>**risk-auditor**: RED — migration prod không revert bằng git revert; giữ RED, chỉ REFINE phạm vi. | **solution-architect**: CONCEDE (`scripts/agents/risk-tier.mjs:33 — { tier: 'RED', re: /^supab`)<br>**risk-auditor**: REFINE | ✅ RESOLVED_EVIDENCE | RED đứng — cho phần migration (áp prod, chỉ lùi được bằng migration ngược). Client PR = AMBER. Architect CONCEDE với evidence risk-tier.mjs:33. RED → cần Cuong duyệt trước /ship. |

### 🔶 Cần anh quyết (1)

**D2 — Cơ chế ship: 1 migration atomic áp cùng lúc, hay bắt buộc thứ tự deploy-web-live-trước → áp REVOKE sau? (Crux factual: bundle CŨ select('*') sau REVOKE → 42501 chết cả query, hay degrade êm?)**

- `solution-architect`: Atomic 1 PR; sau r2: nhận ordering là 'vệ sinh free' nhưng xếp Sự cố 1 xuống lỗi mỹ phẩm vì tin claim degrade-êm (risk#3 r1 của risk-auditor).
- `ui-ux-critic`: Cùng 1 PR (blocker #1); r2: 'same PR' và 'sequenced apply' tương thích — nghiêng theo risk-auditor về thứ tự áp.
- `risk-auditor`: r1: expand-then-contract 2 file; r2 REFINE: 1 file đủ, bất biến là THỨ TỰ — deploy web narrow, xác nhận live, RỒI áp REVOKE. Lưu ý r2 tự sửa r1: bundle cũ select('*') → 42501 (KHÔNG degrade êm như risk#3 r1 đã viết).
- `pre-mortem`: HOLD: select('*') → whole-request 403 → throw → bảng đấu live trắng trong cửa sổ 3-5 phút. PostgREST schema cache role-agnostic, không trim '*' theo role. Sự cố 1 VẪN đứng cho cả scope invite_code-only. Ordering bắt buộc, '1 PR' không tự cưỡng chế thứ tự vì migration đi Management API out-of-band.


