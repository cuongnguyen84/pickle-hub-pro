# PERF-05 — CWV p75 Việt Nam field data, before/after perf (#389 + PERF-04 #417)

Ngày đọc: 2026-07-28 (predicate ≥7 ngày RUM sau #417 20/07 → thoả từ 27/07).

## Verdict: GIỮ

- Không có bằng chứng regression từ #389 (bundle −29% first-paint, 18/07) lẫn #417 (ảnh, 20/07).
- LCP p75 mobile cải thiện 2684ms → 2423ms và **vượt vào ngưỡng GOOD** (target ≤2500ms) ngay khi cửa sổ sau mới chứa ~5–7 ngày post-deploy — hiệu ứng thật nhiều khả năng lớn hơn con số này.
- CLS p75 mobile **0.68 → 0.67, POOR ở cả hai cửa sổ** (target ≤0.1) — vấn đề CÓ SẴN từ trước hai deploy, không phải hồi quy → không thuộc diện rollback, tách thành finding riêng (dưới).

## Số liệu

### CrUX field data (phone, origin `https://www.thepicklehub.net`, ~95% traffic VN nên đại diện VN)

| Metric (p75) | Before: 14/06–11/07 (hoàn toàn trước cả 2 deploy) | After: 28/06–25/07 (chứa 5–7 ngày post-deploy) | Target | Kết luận |
|---|---|---|---|---|
| LCP | 2684 ms (needs improvement) | **2423 ms (good)** | ≤2500 ms | ✅ đạt, cải thiện |
| CLS | 0.68 (poor) | 0.67 (poor) | ≤0.1 | ❌ trượt — có sẵn, đi ngang |
| INP | — | N/A (CrUX thiếu mẫu) | ≤200 ms | chưa đo được |
| FCP | — | 1.1 s (good, 28d hiện tại) | — | ✅ |
| TTFB | — | 0.6 s (good, 28d hiện tại) | — | ✅ |

PSI "Core Web Vitals Assessment: **Failed**" — hoàn toàn do CLS. Nguồn: pagespeed.web.dev
(28-day, "Many samples") + CrUX Vis history (chỉ có 2 điểm tuần — origin mới đủ traffic
để vào CrUX gần đây; 2 cửa sổ 28 ngày cùng độ dài nhưng overlap 14 ngày, đó là mức "cân"
tốt nhất CrUX cho phép).

### GA4 `web_vital` (n mẫu, cửa sổ 21–27/07 — 7 ngày sau #417)

| Segment | Events |
|---|---:|
| VN + mobile | **1.049** (~200–210/metric × 5 metric CLS/FCP/INP/LCP/TTFB) |
| VN + desktop | 1.411 |
| International + mobile | 130 |
| (not set) | 526 (bắn trước thời điểm đăng ký dimension trong ngày 21/07) |

## Lỗ instrumentation phát hiện & đã vá

**Giả định của mốc ("GA4 dimensions đã đăng ký 21/07") SAI cho web-vitals**: 10 dimension
đăng ký 21/07 toàn bộ là telemetry funnel/badge (UX-06/07: app_surface, journey_id,
market_segment, device_class…). **Không có `metric_name` / `metric_rating`** → GA4 không
tách được LCP/INP/CLS và không đọc được %good, ở MỌI cửa sổ (custom dimension không hồi tố
— bảng exploration xác nhận: event trước giờ đăng ký = `(not set)`). Vì vậy nhánh GA4 của
mốc này về cấu trúc là không trả lời được — CrUX ở trên là nguồn verdict.

Đã đăng ký bổ sung 28/07 (property p522556358, event-scoped): `metric_name`,
`metric_rating`. Từ 2026-08-05 đọc được VN+mobile theo từng metric; quy tắc:
**%good ≥75% ⇔ p75 đạt target** (đúng chuẩn ngưỡng rating của web-vitals/CrUX).
→ mốc PERF-05B trong docs/milestones.md.

Ahrefs VN: bỏ, có chủ đích — Ahrefs Web Analytics không có CWV; filter VN của GA4
`market_segment` (geo server-side qua `/api/rum-context`) mạnh hơn country của Ahrefs.

## Finding mới (P1, ngoài phạm vi mốc): CLS p75 mobile ~0.67

Gấp ~6.7× target, đủ tệ để Google đánh Failed toàn origin. KHÔNG phải do 2 PR perf
(flat trước/sau). Nghi phạm chính: `web-vitals` đo CLS trọn vòng đời trang — SPA
soft-navigation + feed infinite scroll tích shift mà lab/Lighthouse (chỉ đo initial load,
đã có aspect-ratio) không thấy. Cần `/idea` riêng: bắt attribution
(`onCLS` với `reportAllChanges` / LayoutShift sources) trước khi sửa mù.
