# Handoff — 2026-07-16 → next session

State of the roadmap after a large day: **37 tasks done**, `main` green, all
migrations applied + ledgered, ledger fully reconciled (283=283).

## Do these FIRST (they need you, Cuong — not code)

1. **Merge PR #345 (PERF-02).** CI is green; it's held only for a manual
   check. Open the preview, load a Team Match tournament, and click through
   a few dialogs (register team, view match, settings, generate matches) —
   they're now lazy-loaded. If they open cleanly, merge. If you trust CI,
   just merge.
2. **Test the admin push broadcast** (`/admin/push-notification`) — send one
   real broadcast. The confirm dialog should show the true device count
   (BE-02); the result toast should report `pruned` dead tokens.
3. **Verify the first GA4 funnel events** (BASE-02) after any real
   registration/publish: look for `player_registration_*` /
   `organizer_event_*` in GA4. A fresh session can drive this with Chrome.
4. **Two product decisions that unblock work:**
   - **SEC-01**: when does the SwiftUI app replace the Capacitor iOS app on
     the App Store? If < ~2 months → risk-accept, skip the 4-day Capacitor
     token-in-URL fix. If no date → do Universal Links + PKCE. Your call
     decides whether SEC-01 gets built.
   - **SEO-02 (full)**: the 3 EN-blog surfaces share the same slug set but
     disagree on ~37 `<title>` values (`BLOG_POST_META.title` = full title;
     `metadata.metaTitleEn` = shorter SEO title). To finish SEO-02 (generate
     everything from one source, drop the blog checklist 5→3) someone must
     pick which title is the SSR `<title>`. Recommendation: `metaTitleEn`.
     Confirm the 37 changes are wanted, then it can be generated.

## Ready to start now (no blockers, high value)

Ordered by my recommendation:

1. ~~**CLOSE-01**~~ — **done (PR #350)**: `src/pages/preview/` deleted
   (~3.9k lines, 12 routes; shared `Countdown` + `formatDate/Time/Relative`
   helpers extracted first). `PublicProfile.tsx` was already gone — stale
   marker comment removed. Total gz JS 1950.0 → 1904.0 KB. Deletion was
   indeed the real bundle-size lever.
2. **QA-03** — RLS/auth matrix + concurrency tests against a disposable
   Supabase. Natural follow-on to the DB-00/01/02 race work; solidifies it.
3. **BE-03** — pin one supabase-js version (currently @2.39.0 ×37, @2 ×33,
   @2.89.0 ×1). Staged + canary per the roadmap note — do it in a quiet
   window, one function group at a time, not between big pushes.
4. **SEO-03 (finish)** — the slug-parity guard shipped; the rest (canonical,
   hreflang reciprocity, schema, bot-200 CI checks) is still open.

## Deferred on purpose (need a dependency first)

- **DS-02/03/04** — wait for real UI work to carry them (DS-01 token list is
  done and ready to reference).
- **A11Y-02** (44px touch targets) / **A11Y-04** (axe tests) — need DS-03
  components. **A11Y-05** blocked on test devices.
- **PERF-04/05** — image/font optimization + CWV field validation; PERF-05
  needs a few weeks of the BASE-03 RUM data first.
- **OPS-04** — SLO dashboards; wait for BASE-02 funnel data to accumulate.
- **UX-01..09** — the whole Phase 3 organizer/player UX arc; UX-09 is
  blocked on usability participants (**BASE-07**, also blocked — recruiting).
- **ARCH-02..05** — domain refactors; ARCH-04 (shared scoring core) needs
  per-format characterization tests first (QA-07 did TeamMatch; the other
  three formats still need theirs).
- **QA-02/04/05** — lint-green (`@ts-nocheck` removal, 267 grandfathered
  errors), E2E, visual regression. Large, do as dedicated efforts.
- **SEO-04** — split the 2,435-line render/index.ts by domain (blog-meta.ts
  extraction started this).
- **OPS-02** — restore drill; needs a disposable Supabase project + a window
  (has a cost).

## Operational notes for whoever picks this up

- **Migrations**: apply via Management API query endpoint, prepend
  `SELECT 1;`, always verify + ledger in the same session, apply BEFORE
  merging if a function build depends on the SQL. Never `db push
  --include-all`. Full procedure: `docs/ops-runbook.md`. SEC-06 now fails CI
  on any drift, so an un-ledgered migration will be caught.
- **Bundle budget** is 1970 KB and **holds** — the ratchet-down plan was
  wrong (splitting doesn't shrink the sum). Reductions come from CLOSE-01 /
  dependency cuts. Rule lives in `docs/perf-budgets.md`.
- **Playwright smoke on main** flakes on the deploy-race ~1 in 2 pushes;
  rerun once and it goes green. The new scroll assertions + chunk-error deny
  list (from the Codex round) are real gates now — a red there is NOT the
  flake, investigate it.
- **PR quality gate lints changed files strictly** — touching a file with
  grandfathered `any`/hook warnings means fixing them in that PR.
- **Local prod-build verification** needs `VITE_*` env; fetch the anon key
  from the Management API `/api-keys` endpoint at runtime.
- Reference docs written today: `ops-runbook.md`, `architecture-boundaries.md`,
  `journey-screens.md`, `design-tokens.md`, `perf-budgets.md`,
  `seo-content-manifest-spec.md`, `slo.md`,
  `audits/review-request-2026-07-16.md`.
- **4 Dependabot PRs (#247–250)** are all-red major bumps (react-router 7,
  recharts 3, eslint 10, react) — each is a real migration, do them
  individually, not as a batch. Suggested order: eslint 10 → recharts 3 →
  react/react-router 7 (own session).

## Memory pointers (auto-loaded next session)

`session-2026-07-16b-shipped`, `session-2026-07-16c-phase25`,
`db-00-race-verdicts` capture the day's decisions and gotchas.
